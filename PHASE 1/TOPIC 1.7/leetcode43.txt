✅ LeetCode — Multiply Strings (LC 43)

🔹 Problem Statement (Simplified)
Given two non-negative integers as strings num1 and num2, return their product as a string.
Do not convert to int/long directly (can overflow).

🔹 Example

Input:  num1 = "123", num2 = "456"
Output: "56088"


🔹 Interview-Preferred Approach: Array Multiplication (Like manual math)
Why this?

No overflow risk

Works for very large numbers

Standard interview method

🔹 Algorithm
Create int[] res = new int[n1 + n2]
Multiply each digit from right to left
Store at position i + j + 1
Add carry to previous index
Convert array to string, skip leading zeros

🔹 Java Code (BEST ANSWER)

class Solution {
    public String multiply(String num1, String num2) {
        int n1 = num1.length(), n2 = num2.length();
        int[] res = new int[n1 + n2];

        for (int i = n1 - 1; i >= 0; i--) {
            for (int j = n2 - 1; j >= 0; j--) {
                int mul = (num1.charAt(i) - '0') * (num2.charAt(j) - '0');
                int sum = mul + res[i + j + 1];

                res[i + j + 1] = sum % 10;
                res[i + j] += sum / 10;
            }
        }

        StringBuilder sb = new StringBuilder();
        for (int r : res) if (!(sb.length() == 0 && r == 0)) sb.append(r);
        return sb.length() == 0 ? "0" : sb.toString();
    }
}


⏱ Time & Space Complexity
Time: O(n1 × n2)
Space: O(n1 + n2)

🔥 Interview Tip
If asked:
Why not use built-in number conversion?
Answer:
Input size can be larger than 32/64-bit limit. This method avoids overflow and mimics manual multiplication safely.

📌 Quick Revision Table
Approach	Time	Space
Convert to number & multiply	❌ Overflow risk	O(1)
Optimized (Array math)	O(n1 × n2)	O(n1 + n2)